# Unit 6a — adopting dispatch identity

Date: 2026-08-22. Revision 1. The framework's dispatch-identity slice is merged: every
turn-product block carries its summoning frontier's id, the duplicate-turn window is
closed, and the framework's coordination document (docs/coordination in the framework
repository) names this unit's items. This unit adopts all of it and lifts the
registration floor with the sound gate the anchor makes possible.

## The unit's contract

1. **The mechanical adoption**, per the coordination document: the Block-struct field
   joins every construction site; both scripted providers emit the trailing done real
   wires send; the echo tolerances and their counters are REMOVED (the framework fix
   shipped); the duplicate-turn canary is un-ignored and must pass.
2. **The floor lifts.** Registration accepts tools above member again. Admission
   gains the anchor gate per the coordination document's recipe: the call block's
   dispatch anchor loads the summoning message in one step; the gate's reading is
   the MINIMUM of the summoner's stored debt authority (its own fold applies) and
   the authorities of every chat message between the summoner and the call — the
   walk that was refuted without a lower bound is sound with one. A null anchor
   (out-of-band) reads as the floor. Decision 0043 gains its dated closure-of-the-
   closure: the mechanism arrived, the floor lifted.
3. **The /report prompt gate** (pre-v1 item): the /report bullet leaves
   prompts/assistant.md into decision 0046's gated set with its trigger (unit 6b
   ships the report tool), so the prompt can reach production before the tool.
4. **AC5-era tests return**: the test-registered admin tool is admitted for an
   admin-summoned turn and declined for a member-summoned one, under the anchor
   gate, including the absorbed-message shapes the old walk failed (narrated turn,
   between-rounds absorption) — each pinned end to end with the production event
   order.

## Acceptance criteria

- **AC1** Workspace suite green both modes; the canary passes un-ignored; no echo
  tolerance remains (grep-proven).
- **AC2** The anchor gate: admitted-above-floor and declined-below-requirement both
  pinned, plus the two absorbed shapes; a null anchor reads as the floor — pinned.
- **AC3** Registration accepts an admin-level tool; the floor refusal and its test
  are gone; 0043's closure note is dated.
- **AC4** The /report bullet is out of the prompt and recorded in 0046 with its
  trigger; the prompt suite passes.
- **AC5** Clippy denied, fmt, doc denied (workspace and per package), vocabulary and
  secret scans clean; no new dependency.
